# 道の駅ナビ｜東北スタンプラリー＆ルート検索

東北6県（青森・岩手・宮城・秋田・山形・福島）の**全道の駅**を地図上で管理し、
訪問記録・スタンプ取得・県別達成率・営業時間表示・週末の周遊ルート提案までできるWebアプリ（PWA）です。

- APIキー不要・無料で動作（地図は OpenStreetMap、経路計算は OSRM 公開デモサーバー、
  名称・住所検索は OpenStreetMap Nominatim と国土地理院 住所検索API）
- スマートフォン優先UI・全画面地図モード対応
- 訪問記録・保存ルートはお使いの端末内（localStorage）にのみ保存（サーバー送信なし）
- ホーム画面に追加してアプリのように使えるPWA（オフラインでも一覧・記録の閲覧/更新が可能）

---

## 公開URL

<!-- GitHub Pages公開後、実際のURLに置き換えてください -->
https://（ユーザー名）.github.io/tohoku-michinoeki-map/

このURLはスマートフォン・PCのブラウザからそのまま開けます。インストール不要です。
（ホーム画面に追加する場合は「保存」タブ → 「ホーム画面への追加方法を見る」）

## 先行モニター販売サイト

`/monitor/` に「道の駅ナビ 先行モニター」（月額250円・正式版は月額500円を予定）の販売LP・利用規約・プライバシーポリシー・特定商取引法に基づく表記・お問い合わせ・お支払い後のご案内ページを静的HTMLで出力する（`src/monitorSite/`）。
Ownerの確認値とStripe Liveの購入URLが揃うまでは「受付準備中」表示で、申込ボタンは出ない。運用・Live設定手順は [`docs/MONITOR_SALES_RUNBOOK.md`](./docs/MONITOR_SALES_RUNBOOK.md) を参照。

## 開発ブランチについて

- `main`: 現行公開版。GitHub Pagesへ自動デプロイされる
- `v1.0.3-nami`（タグ）: 2026-09-10 実地テスト用に凍結した安定版。変更・削除しない
- `v1.1.0-rc1`（タグ）: 2026-09-16 Owner iPhone実機QA PASS後に凍結した機能FIX版（Final Gate前の基準点）。
  全国1,237施設・地域→県→地図・周辺スポット・ルート・Google Web検索・Googleマップのカテゴリ検索を含む。
  QA配信のみで本番未反映。変更・削除しない
- `product/main`: 将来の製品版（無料/有料プラン、クラウド同期等）を開発するブランチ。
  設計・方針は [`PRODUCT_ARCHITECTURE.md`](./PRODUCT_ARCHITECTURE.md) を参照

## 起動方法（Windows・ローカルで使う場合）

1. **`start-michinoeki-map.bat` をダブルクリック**するだけです。
   - 初回は自動でセットアップ（依存関係のインストール＋ビルド）が走ります。数分かかります。
   - 起動確認後、ブラウザで `http://127.0.0.1:4173` が自動で開きます。
2. **停止方法**: 黒いウィンドウで `Ctrl+C` を押す（または黒いウィンドウを閉じる）。
3. **再起動方法**: もう一度 `start-michinoeki-map.bat` をダブルクリック。2回目以降はすぐ起動します。
4. 同じWi-Fi内の他の端末からは `http://<このPCのLAN IP>:4173` でもアクセスできます
   （起動時にコンソールへ表示されるアドレスを使ってください）。

※ 事前に [Node.js (LTS版)](https://nodejs.org/ja) のインストールが必要です。

## スマホ実機での確認方法（開発中の変更をスマホで見る場合）

修正のたびにPowerShellを手入力する必要はありません。以下の2つをダブルクリックするだけです。

1. **`スマホ実機テスト開始.bat` をダブルクリック**する（本体は `scripts/pretest-start.ps1`）
   - 古いサーバー停止 → ブランチ/コミット確認 → クリーンビルド → プレビュー起動 →
     **HTTPS のテストURL**（Cloudflare Quick Tunnel、アカウント不要）を発行し、
     さらに「HTTPS経由で配信されているビルドID」が「今ビルドしたID」と一致することを
     確認してから、スマホで開くURLを画面に大きく表示します。
   - **スマホでは必ずこのHTTPS URLを開いてください**（現在地機能はHTTPSでのみ動作します）。
     URLは起動のたびに変わります。以前のURLは無効です。
   - スマホの「保存」タブ一番下の「🔧 動作診断」に表示される **ビルドID** が、
     PC画面に表示されたビルドIDと同じなら、検証したものと同じ版を見ています。
     違う場合は診断画面の「最新版に更新」を押してください。
   - LANのHTTP URL（`http://192.168.x.x:4173/`）は内部診断用です。位置情報が使えないため、
     実機テストには使いません。
2. **テストを終了する場合**: `スマホ実機テスト終了.bat` をダブルクリックする。

ビルド失敗・ポート競合・HTTPS URL発行失敗・配信ビルド不一致などが起きた場合は、
画面に日本語で原因と次にやることが表示されます。

PWA（Service Worker）の更新経路は `node scripts/verify_sw_update.cjs` で検証できます
（SWを有効にした永続プロファイルで、旧ビルド→新ビルドへの自動更新と再起動後の維持を確認）。

### 開発者向けコマンド

| コマンド | 内容 |
| --- | --- |
| `npm install` | 依存関係のインストール |
| `npm run dev` | 開発サーバー (http://localhost:5173) |
| `npm run build` | 本番ビルド（`dist/` に出力。`DEPLOY_BASE` 環境変数でサブパス配信にも対応） |
| `npm run preview` | 本番ビルドをローカル配信 (http://127.0.0.1:4173) |
| `npm test` | ユニット/データテスト（Vitest） |
| `npm run e2e` | ブラウザE2Eテスト（Playwright、要 `npx playwright install chromium`） |
| `npm run gen-icons` | アプリアイコン／OGP画像の再生成（`app-icon-source.png` から。要 Python + Pillow） |

`main` ブランチへの push で GitHub Actions が自動的にビルド・GitHub Pages へ公開します
（`.github/workflows/deploy.yml`）。

---

## 収録データについて

### 収録件数（データ確認日: **2026-09-02**）

| 都道府県 | 収録施設数 |
| --- | --- |
| 青森県 | 28 |
| 岩手県 | 39 |
| 宮城県 | 19 |
| 秋田県 | 34 |
| 山形県 | 24 |
| 福島県 | 38 |
| **合計** | **182施設** |

- 国土交通省の**登録数は181駅**（青森28・岩手39・宮城19・秋田34・山形24・福島37）です。
- 本アプリの施設数が182件なのは、**「安達」（福島県二本松市）が1登録・上下線2施設**であり、
  実際に別の場所にある2施設を両方収録しているためです（件数合わせのための削除・追加は行っていません）。

### 開業前施設の扱い

- **道の駅「石川」**（福島県石川郡石川町・第64回登録・令和8年度オープン予定）は
  「登録済み・開業前」として収録しています。地図ではグレーのピンで表示され、
  **ルート提案の候補からは常に除外**されます。座標は住所ジオコーディングによる概略値です。

### 採用した情報源（一次情報優先）

1. **国土交通省「道の駅」一覧**（2025-12-09更新・第64回登録反映・全国1,231駅）
   https://www.mlit.go.jp/road/Michi-no-Eki/list.html （XLS一覧から東北6県を抽出・全駅照合）
2. **全国「道の駅」連絡会 公式ホームページ**（各駅の名称・住所・座標・公式リンク・営業時間）
   https://www.michi-no-eki.jp/
3. **国土交通省 報道発表（第64回登録）**（開業前の「石川」の所在地・開業予定）
   https://www.mlit.go.jp/report/press/road01_hh_002029.html
4. **Wikidata**（読み仮名・公式サイトURLの補完。座標は公式ポータルと突合し2km以上の乖離ゼロを確認）
5. **国土地理院 住所検索API**（開業前施設の座標付与）

### データの設計と更新方法

- マスターデータは `src/data/stations.json` に**ユーザーの訪問記録と完全分離**して保存しています。
- 各駅は永続ID（例: `mne-18900`）を持ち、訪問記録はIDのみを参照するため、
  **駅データを更新しても既存の訪問記録は失われません**。
- 更新するには `src/data/stations.json` を編集（または再生成）して `npm run build` を実行します。
  追加時は `id / name / pref / city / address / lat / lng / status / infoUrl / verifiedAt / sources` が必須です。
- データの整合性は `npm test` の**データテスト**（ID重複・座標範囲・県別件数照合など）で自動検証されます。

### 営業時間・休館日について

- 営業時間データは `src/data/hours.json` に182施設分収録しています（情報源: 全国「道の駅」連絡会）。
- 確証が持てない駅・情報が見つからない駅は**推測せず「要確認」表示**にしています
  （営業時間を勝手に埋めることはしていません）。
- 表示ステータス: 営業中 / まもなく終了（閉店60分前〜） / 営業時間外 / 開業前 / 要確認。
  時刻はブラウザの設定に関わらず**日本時間（Asia/Tokyo）固定**で判定します。
- 冬季閉鎖が確認されている駅（わきのさわ・かわうち湖・しんごう・みんまや・こどまり・こさか七滝）には注記を表示しています。
- 営業時間は変わりやすいため、出発前に必ず詳細カードの「公式情報を見る」から最新情報を確認してください。

---

## 地図の表示設定

- 初期状態では東北6県**全182施設を個別のアイコンで表示**します（広域ではまとめ表示に切替も可能）。
- ズームに応じてアイコンサイズ・駅名ラベルの表示量が自動で変わります（タップ領域は常に約44×44px確保）。
- 「まとめて表示（クラスター）」「駅名表示（自動/常に表示/非表示）」は地図の凡例パネルから切り替えでき、
  設定は端末に保存され次回起動時も維持されます。

## 記録のバックアップ・復元

- 訪問記録・スタンプ・保存ルート・地図設定は「保存」タブの「記録をバックアップ」からJSONファイルに書き出せます。
- 機種変更・別ブラウザへの移行時は、同じ画面の「バックアップを復元」からファイルを選択してください。
  「統合」（既存記録を残しつつ合わせる）と「上書き」（すべて置き換える）を選べます。
- バックアップの中身が壊れている・形式が不正な場合は復元前にエラー表示し、既存の記録は変更しません。

## 訪問記録の保存場所

- 訪問状態・スタンプ・保存ルート・地図設定はすべて**ブラウザの localStorage** に保存されます。
  - `tohoku-me:visits:v2` … 訪問・スタンプ記録（旧 `v1` からは初回起動時に自動移行）
  - `tohoku-me:routes:v1` … 保存ルート
  - `tohoku-me:trip:v1` … 旅行中の進行状態
  - `tohoku-me:map-settings:v1` … 地図の表示設定
  - `tohoku-me:manual-route-draft:v1` … 「地図から選ぶ」の選択途中（完成・キャンセルで削除）
- サーバーには何も送信されません。ブラウザのサイトデータを消すと記録も消えます（事前にバックアップ推奨）。
- 「保存」タブの「すべての記録を初期化する」から（確認画面つきで）リセットできます。

## ルート作成の2つの方式

「コース」タブでは、毎回最初に作り方を選びます。

1. **おすすめコース**（自動選定）: 時間や条件に合わせて、アプリが回りやすい道の駅を自動で選びます。
2. **地図から選ぶ**（手動選択）: 地図上で行きたい道の駅を自分でタップして選び、その駅だけで周遊コースを作ります。
   - 選択モード中はマーカーをタップしても**訪問記録の色は変わりません**（選択の追加/解除のみ）。もう一度タップすると解除できます。
   - 選んだ駅には①②③の番号が付き、下部の一覧シートで確認・削除・並べ替えができます（最低2駅・最大20駅）。
   - 回る順番は「選んだ順番のまま」「回りやすい順に自動調整」（初期値）から選べます。
   - 選択した全駅が設定時間に収まらない場合は駅を黙って削除せず、超過時間を明示したうえで
     「時間を変更する／選択駅を減らす／時間内に回れる駅だけで作成（除外候補を提示して確認後に除外）／
     時間を超えてこのまま作成」から選べます。
   - 到着予定時刻が営業時間外・要確認になる駅も自動では除外せず、警告のうえで「このまま含める／選択から外す／
     公式情報を見る」から選べます。
   - 選択途中の内容は端末に自動保存され、途中で閉じても次回起動時に「続けますか？」と確認されます
     （バックアップにも含まれます）。

どちらの方式で作ったコースも、この後の仕組みは共通です。

- **使用可能時間 = 移動時間 + 各駅の滞在時間 + （帰着ありの場合）帰路 + 安全マージン** をすべて含めて計算します。
- 経路・所要時間は可能な場合 **OSRM（キー不要の公開デモサーバー）による実道路ベースの推定**を使用します。
  応答が得られない場合のみ、直線距離×1.35の道路補正・一般道40km/h・高速60km/h想定・区間ごとに5分のバッファ・
  冬季（11〜3月）2割増しの**概算**にフォールバックします。どちらを使ったかは画面上に明記されます
  （渋滞・通行止め等のリアルタイム情報は反映されません）。
- おすすめコースでは3種類のコース（がっつり周遊/バランス/ゆったり）を安全マージン付きで提案します。
- 開業前・休止中の駅は候補から除外。時間制限を超えるルートは（手動選択で明示的に許可した場合を除き）提案しません。
- 各駅から次の駅への移動は「Googleマップでナビ開始」から確認・利用できます
  （経由地が多い場合はGoogleマップの上限に合わせて区間分割されます）。

## PWA・オフライン動作

- ホーム画面に追加すると全画面アプリとして動作します（Web App Manifest + Service Worker）。
- オフラインでも: 道の駅一覧・訪問記録の閲覧/更新・保存ルートの閲覧が可能です。
- オフラインでは利用できないもの: 地図タイルの新規読み込み・公式ページ・Googleマップ・住所検索・経路計算
  （画面上部にオフライン表示が出ます）。
- キャッシュは新しいビルドを検知すると自動更新されるため、古い道の駅データが残り続けることはありません。

## 技術構成

React 18 + TypeScript + Vite / Leaflet + OpenStreetMap + leaflet.markercluster /
OSRM（経路計算・キー不要） / vite-plugin-pwa (Workbox) / Vitest / Playwright /
GitHub Actions + GitHub Pages（公開）。地図・ルート計算ともにAPIキー・課金は不要です。
